services:
  starmap:
    build: .
    ports:
      # Host port is configurable via .env (STARMAP_PORT); container stays 8080
      - "${STARMAP_PORT:-8080}:8080"
    env_file: .env
    volumes:
      - ./data:/app/data   # SQLite file + JSON data persisted on host
    restart: unless-stopped
