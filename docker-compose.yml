version: '3.9'

services:
  starmap:
    build: .
    ports:
      - "8080:8080"
    env_file: .env
    volumes:
      - ./data:/app/data   # SQLite file + JSON data persisted on host
    restart: unless-stopped
