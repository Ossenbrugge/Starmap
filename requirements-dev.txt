# Development dependencies (lint, type-check, test, security scan).
# Install with: pip install -r requirements-dev.txt
# Runtime deps live in requirements.txt; the test suite's own list is tests/requirements.txt.

-r requirements.txt
-r tests/requirements.txt

# Lint / format / types (matches .pre-commit-config.yaml and CI)
ruff>=0.4.4
flake8>=6.0.0
mypy>=1.10.0

# Coverage
coverage[toml]>=7.0.0

# Security scanning (tox -e security)
bandit>=1.7.0
safety>=2.3.0

# Workflow
pre-commit>=3.2.0
tox>=4.4.0
