# Starmap testing requirements
# The suite runs in-process against create_app() with a fixture SQLite DB.

# Core application dependencies exercised by the tests
Flask>=3.0.0
Werkzeug>=3.0.1
Flask-Login>=0.6.3
PyJWT>=2.9.0

# Testing framework and utilities
pytest>=7.0.0
pytest-cov>=4.0.0
pytest-mock>=3.10.0
